project.basedir=${project.basedir}
project.screenshotdir=${project.basedir}/screenshots
project.supportdir=${project.basedir}/support
scm.branchname=${scmBranch}
scm.revnumber=${buildNumber}
testenv.configdir=${sys:user.dir}/src/main/resources/config
allure.resultdir=${allure.results.directory}
application.name = Killer App
application.version = 1.6.2

application.title = ${application.name} ${application.version}
